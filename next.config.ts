/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["dzwhallxkdoucotgdhdn.supabase.co"], // add your Supabase storage host here
  },
  experimental: {
  },
};

module.exports = nextConfig;
